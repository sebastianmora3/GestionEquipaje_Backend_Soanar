# Nombre de la aplicación
spring.application.name=GestionEquipaje

# Configuración de PostgreSQL Database (actualizada para Azure)
#spring.datasource.url=jdbc:postgresql://myserverpostgres.postgres.database.azure.com:5432/Luggage
spring.datasource.url=jdbc:postgresql://mypostgreserver.postgres.database.azure.com:5432/Luggage
spring.datasource.username=admin1
spring.datasource.password=Fabrica2024
spring.jpa.properties.hibernate.format_sql=true

# Mostrar las consultas SQL en la consola
spring.jpa.show-sql=true

# Configuración de JPA (usar Hibernate para manejar la base de datos)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Configuración del pool de conexiones (opcional)
spring.sql.init.mode=always
spring.datasource.driver-class-name=org.postgresql.Driver
